---
const { title, description, href, src } = Astro.props;
---

<a
  class="border border-gray-400 flex flex-col gap-y-4 hover:shadow-md transition-all duration-100 ease-in cursor-pointer"
  href={href}
>
  <img class="w-full h-48 object-cover" src={src} />
  <div class="flex flex-col gap-y-1 p-4 relative pb-10">
    <h3 class="text-lg">{title}</h3>
    <p class="text-gray-500 text-sm">{description}</p>
  </div>
</a>
